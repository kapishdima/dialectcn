import { atom } from "jotai";

export const installDialogOpenAtom = atom(false);

export const likeTriggerAtom = atom(0);

export const copyTriggerAtom = atom(0);

export const sidebarHiddenAtom = atom(false);

export const helpOpenAtom = atom(false);
